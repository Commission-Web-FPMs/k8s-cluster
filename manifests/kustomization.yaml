apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - argocd/chart.yaml

  # MetalLB : attribue des IP de type LoadBalancer aux services
  - metallb/

  # S3 : permet d'avoir du stockage
  # Devrait être remplacé par un accès au NAS
  - csi-s3/
